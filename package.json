{
  "name": "entelliextract-test-stub",
  "version": "1.0.0",
  "description": "Test automation stub for EntelliExtract API - extraction service with S3 sync, checkpointing, load testing, and reporting",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "run": "npm run build && npm start",
    "sync": "npm run build && node dist/index.js sync",
    "extract": "npm run build && node dist/index.js run",
    "report": "npm run build && node dist/index.js report",
    "test-runner": "node test-runner-server.mjs"
  },
  "keywords": [
    "entelliextract",
    "test-automation",
    "s3",
    "extraction",
    "benchmark"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "commander": "^12.1.0",
    "dotenv": "^16.4.5",
    "js-yaml": "^4.1.0",
    "p-queue": "^8.0.1",
    "simple-statistics": "^7.8.3",
    "undici": "^7.21.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.17.6",
    "typescript": "^5.6.3"
  }
}
