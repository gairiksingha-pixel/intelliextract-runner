# EntelliExtract API credentials (required for extraction)
# Copy this file to .env and fill in your values:  cp .env.example .env
# API base URL only (no trailing slash; the client appends /api/v1/...)
ENTELLIEXTRACT_BASE_URL=https://your-api.example.com

# --- Option A: Plain secrets (simple, but avoid committing .env) ---
# ENTELLIEXTRACT_ACCESS_KEY=your-access-key
# ENTELLIEXTRACT_SECRET_MESSAGE=your-secret-message
# ENTELLIEXTRACT_SIGNATURE=your-signature

# --- Option B: Fernet-encrypted secrets (recommended) ---
# 1. Generate a key: Python: Fernet.generate_key()  or  Node: Fernet.generateKey()
# 2. Encrypt each secret with that key and paste the token below.
# 3. Put FERNET_KEY in .env (or in a secure place like CI secrets). App decrypts at runtime.
FERNET_KEY=dummy-base64url-key-replace-with-your-32-byte-fernet-key
ENTELLIEXTRACT_ACCESS_KEY_ENCRYPTED=gAAAAABldummy1replace_with_your_fernet_encrypted_access_key_token
ENTELLIEXTRACT_SECRET_MESSAGE_ENCRYPTED=gAAAAABldummy2replace_with_your_fernet_encrypted_secret_message_token
ENTELLIEXTRACT_SIGNATURE_ENCRYPTED=gAAAAABldummy3replace_with_your_fernet_encrypted_signature_token

# AWS credentials for S3 sync (optional if only running extraction)
# --- Option A: Plain ---
# AWS_ACCESS_KEY_ID=your-aws-access-key-id
# AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# --- Option B: Encrypted (use same FERNET_KEY as above) ---
AWS_ACCESS_KEY_ID_ENCRYPTED=gAAAAABldummy4replace_with_your_fernet_encrypted_aws_access_key
AWS_SECRET_ACCESS_KEY_ENCRYPTED=gAAAAABldummy5replace_with_your_fernet_encrypted_aws_secret_key
AWS_REGION=us-west-2

# S3 bucket and tenant/purchaser layout (single bucket with tenant/purchaser folders)
S3_BUCKET=your-bucket-name
# JSON map: tenant folder name -> list of purchaser folder names
S3_TENANT_PURCHASERS={"tenant-a":["PURCHASER_1","PURCHASER_2"],"tenant-b":["PURCHASER_1"]}

# Optional: override config file path (default: config/config.yaml)
# CONFIG_PATH=./config/config.yaml
